# claude_in_container
Dockerized Claude Code environment with persistent OAuth authentication. Mounts the user home directory so credentials and configuration survive container restarts. Works with Claude.ai Pro/Team subscriptions.
